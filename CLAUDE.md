# 앵그리모티 (AngryMoti) — 작업 규칙

## 플랫폼 작업 정책 (중요)

**현재 단계에서는 iOS만 작업한다.** 버그 수정이든 신규 기능이든, 지시가 따로 없으면
`TimeLock/` (iOS) 만 수정하고 `android/` 는 건드리지 않는다.

이유: 한 건마다 두 플랫폼을 동시에 맞추면 작업 시간이 배로 들고, 이식 과정에서
플랫폼 고유의 파생 버그가 새로 생긴다(실제로 안드로이드 쪽에서 DatePicker UTC 환산
누락, 표시값/저장값 불일치 등이 그렇게 발생했다).

**iOS를 기준 구현으로 완전히 안정화한 뒤**, 별도 지시를 받아 안드로이드를 iOS 기준으로
한 번에 이식(클로닝)한다. 그때 정책·로직·UI 시스템 모두 iOS를 정본으로 삼는다.

- 안드로이드는 현재 **부분적으로만** 수정된 상태다. 나중에 이식할 때는 개별 diff를
  따라가지 말고 iOS 현재 상태를 기준으로 전면 대조할 것.
- 안드로이드 CI(`android-build.yml`)는 계속 돌지만, 손대지 않으므로 그린 상태가 유지된다.

## 저장소 / 브랜치

- 저장소: `singlemarks-hash/routine`
- 작업 브랜치: `claude/ecstatic-heisenberg-dg5zzg`
- 모든 변경은 커밋 → 브랜치 푸시 → `main` fast-forward 머지 → `main` 푸시 순으로 진행한다.

## 검증

- 로컬 빌드 불가(샌드박스). **CI가 유일한 컴파일 검증 수단**이다.
  - iOS: `.github/workflows/ios-build.yml` (Xcode 시뮬레이터 빌드)
  - Android: `.github/workflows/android-build.yml` (`compileReleaseKotlin` + Room KSP)
- `project.pbxproj`는 로컬에서 직접 수정하지 않는다.

## 커뮤니케이션

- 모든 응답은 한국어로 작성한다.

## 도메인 핵심 — 예약은 "종료일 있는 알람시계"

`Reservation` 한 건이 알람시계와 같은 필드를 가진다.

| 개념 | 필드 |
|---|---|
| 시각 | `startMinute` (자정 기준 분) |
| 반복 요일 | `repeatWeekdays` (1=일 … 7=토). **요일 전체(7개) = "매일"** |
| 시작 게이트 | `createdAt` — 이 날짜부터 발생 |
| 종료 게이트 | `endDate` — 이 날짜까지. nil = 무기한 |
| 모드 마커 | `oneOffDate` — 있으면 "매일/하루" 모드, 없으면 "요일 반복" 모드 |
| 노쇼 책임 기준 | `accountableFrom` — 이 시각 이전 발생분은 벌점 없음 |

**발생 판정은 반드시 `occurrence(on:)` 하나로만 한다.** 화면에서 요일을 따로 검사하면
시작일·종료일 게이트가 빠져 실제 알람과 화면이 어긋난다(과거에 그렇게 버그가 났다).

### 건드릴 때 주의할 불변식

1. **`createdAt`은 첫 발생이 지난 뒤에는 절대 바뀌면 안 된다.**
   노쇼 복구 루틴이 `scheduledAt < createdAt`인 노쇼를 "잘못된 기록"으로 보고
   세션과 벌점을 삭제한다. 시작일을 미룰 수 있으면 과거 벌점이 전부 지워지는
   회피 경로가 된다. → 이미 시작한 예약은 시작일 잠금(UI + 저장 경로 이중 방어).

2. **`accountableFrom`은 생성/편집 시 `max(지금, 시작일)`.**
   시작일 자정으로 두면 오늘 만든 예약이 오늘 아침 발생분까지 소급 노쇼로 잡힌다.

3. **주간 무한 반복 알람 트리거는 종료일을 표현할 수 없다.**
   `endDate == nil` 이고 시작일이 이미 지난 예약에만 쓴다. 그 외에는 창 안의
   실제 발생분만 구체 예약한다. (하루짜리가 영원히 울리던 원인)

4. **되돌리기(노쇼 복구·그룹 정리)로 점수 이벤트를 지울 때는 클라우드 사본도 지운다.**
   로컬만 지우면 다음 동기화에서 되살아나 "세션 없는 유령 벌점"이 된다.

5. **그룹 예약을 삭제할 때는 알람도 함께 취소한다.**
   예약 없이 알람만 남으면 라우팅 대상이 없어 끌 수 없는 알람이 된다.
